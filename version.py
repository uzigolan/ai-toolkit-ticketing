"""
Single source of truth for the app's identity and version.

Bump APP_VERSION when you change behaviour people depend on -- it is shown in
the UI and stamped into exported eval cases, so a case can be traced back to the
ticketing build that produced it.
"""
APP_NAME = "RAD AI Ticketing Center"
APP_VERSION = "1.0.0"
